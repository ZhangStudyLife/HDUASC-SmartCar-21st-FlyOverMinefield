# 空地通信互传数据手册

## 协议

统一用 `0x06 RUN_DATA`，float 数组，不 ACK、不重试、不影响参数通道。RUN_DATA 以 200Hz（5ms）双向互发，收发函数都是 `air_comm_send_run_data()`。心跳仍为 200ms，离线判定仍为 600ms，运行数据新鲜度超时仍为 100ms。

车端第 11 个 float 为基于 `tick_1000us_cnt` 的 1ms 单调时间戳，每个正常发送周期约增加 5ms。发送使用非阻塞队列；队列压力出现时仅用最新 RUN_DATA 替换尚未发送的旧 RUN_DATA，不覆盖心跳和 ACK。

摄像头帧驱动算法、车灯融合、投影估计和图像控制链为 100Hz；Camera SPI 轮询服务为 200Hz。投影姿态历史扩展为 4 个 100Hz 样本，补偿延迟仍约 40ms。

## 数据协议

| 方向 | 索引 | 含义 | 单位 |
|------|------|------|------|
| **车→飞机** | 0~3 | 四轮编码器 count_raw | 脉冲/周期 |
| | 4~6 | 加速度计 accx/accy/accz | m/s² |
| | 7~9 | 陀螺仪 gyrox/gyroy/gyroz | °/s |
| **飞机→车** | 0~3 | TOF1~4 高度 | mm |
| | 4~6 | 加速度计 accx/accy/accz | m/s² |
| | 7~9 | 陀螺仪 gyrox/gyroy/gyroz | °/s |

## 车端（car_loop.c）

**发送**（200Hz 非阻塞任务内）：

```c
float car_data[10];
car_data[0] = encoder_left_front.count_raw;
car_data[1] = encoder_right_front.count_raw;
car_data[2] = encoder_left_rear.count_raw;
car_data[3] = encoder_right_rear.count_raw;
car_data[4] = g_imufilter_1000hz.accx;
car_data[5] = g_imufilter_1000hz.accy;
car_data[6] = g_imufilter_1000hz.accz;
car_data[7] = g_imufilter_1000hz.gyrox;
car_data[8] = g_imufilter_1000hz.gyroy;
car_data[9] = g_imufilter_1000hz.gyroz;
air_comm_send_run_data(car_data, 10);
```

**接收**（全局变量 + 回调 + 注册）：

```c
volatile float g_air_tof1_height_mm;
volatile float g_air_tof2_height_mm;
volatile float g_air_tof3_height_mm;
volatile float g_air_tof4_height_mm;
volatile float g_air_imufilter_1000hz_accx;
volatile float g_air_imufilter_1000hz_accy;
volatile float g_air_imufilter_1000hz_accz;
volatile float g_air_imufilter_1000hz_gyrox;
volatile float g_air_imufilter_1000hz_gyroy;
volatile float g_air_imufilter_1000hz_gyroz;

static void on_air_data(const float *data, uint8 count)
{
    if (count >= 10)
    {
        g_air_tof1_height_mm = data[0];
        g_air_tof2_height_mm = data[1];
        g_air_tof3_height_mm = data[2];
        g_air_tof4_height_mm = data[3];
        g_air_imufilter_1000hz_accx  = data[4];
        g_air_imufilter_1000hz_accy  = data[5];
        g_air_imufilter_1000hz_accz  = data[6];
        g_air_imufilter_1000hz_gyrox = data[7];
        g_air_imufilter_1000hz_gyroy = data[8];
        g_air_imufilter_1000hz_gyroz = data[9];
    }
}

// car_loop_init() 中，air_comm_car_init() 之后
air_comm_set_run_data_callback(on_air_data);
```

## 飞机端（main_cm7_0.c）

**发送**（200Hz 发送槽内，规划仍以 100Hz 更新）：

```c
float air_data[10];
air_data[0] = g_tof1_height_mm;
air_data[1] = g_tof2_height_mm;
air_data[2] = g_tof3_height_mm;
air_data[3] = g_tof4_height_mm;
air_data[4] = g_imufilter_1000hz.accx;
air_data[5] = g_imufilter_1000hz.accy;
air_data[6] = g_imufilter_1000hz.accz;
air_data[7] = g_imufilter_1000hz.gyrox;
air_data[8] = g_imufilter_1000hz.gyroy;
air_data[9] = g_imufilter_1000hz.gyroz;
air_comm_send_run_data(air_data, 10);
```

**接收**（同理）：

```c
volatile float g_car_encoder_left_front;
volatile float g_car_encoder_right_front;
volatile float g_car_encoder_left_rear;
volatile float g_car_encoder_right_rear;
volatile float g_car_imufilter_1000hz_accx;
volatile float g_car_imufilter_1000hz_accy;
volatile float g_car_imufilter_1000hz_accz;
volatile float g_car_imufilter_1000hz_gyrox;
volatile float g_car_imufilter_1000hz_gyroy;
volatile float g_car_imufilter_1000hz_gyroz;

static void on_car_data(const float *data, uint8 count)
{
    if (count >= 10)
    {
        g_car_encoder_left_front  = data[0];
        g_car_encoder_right_front = data[1];
        g_car_encoder_left_rear   = data[2];
        g_car_encoder_right_rear  = data[3];
        g_car_imufilter_1000hz_accx  = data[4];
        g_car_imufilter_1000hz_accy  = data[5];
        g_car_imufilter_1000hz_accz  = data[6];
        g_car_imufilter_1000hz_gyrox = data[7];
        g_car_imufilter_1000hz_gyroy = data[8];
        g_car_imufilter_1000hz_gyroz = data[9];
    }
}

// main() 中，air_comm_air_init() 之后
air_comm_set_run_data_callback(on_car_data);
```

## 注意

- 全局变量加 `volatile`，防止编译器优化
- 200Hz 发送，丢帧不影响，下一帧刷新
- 车端 11-float RUN_DATA 帧总长 54 字节；飞机端 15-float 关键帧总长 70 字节、52-float 诊断帧总长 218 字节。1152000 波特率下均由异步 FIFO 发送，不在控制任务中等待物理发送完成。
